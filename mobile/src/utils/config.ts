export const APP_CONFIG = {
  // Development default; adjust for physical device or emulator as needed (e.g. 10.0.2.2 or LAN IP)
  API_BASE_URL: process.env.EXPO_PUBLIC_API_URL || 'https://absensi.stkip-us.ac.id/api',
};
